#!/bin/bash
set -euo pipefail

# Hardened gcdh launcher - always prefers the project venv Python (3.12)
# This fixes the recurring Python 3.9 vs `str | None` syntax errors when
# launched from Grok TUI, CI, or other short-timeout wrappers.

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd -P)"
VENV_PYTHON="$REPO_ROOT/.venv/bin/python"

if [ -x "$VENV_PYTHON" ]; then
    exec "$VENV_PYTHON" -m code_delegation_harness "$@"
else
    echo "[gcdh] Warning: No local venv found at $VENV_PYTHON, falling back to system python" >&2
    exec python3 -m code_delegation_harness "$@"
fi
